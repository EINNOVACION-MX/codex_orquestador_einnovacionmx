export const PROJECT_SCHEMA_VERSION = 1;
export interface ProjectMetadata { schemaVersion: number; projectId: string; name: string; rootPath: string; initializedAt: string; lastOpenedAt: string; repository?: { root: string; lastIndexedCommit?: string }; stack?: string[]; commands?: Record<string, string>; importantPaths?: string[]; }
export interface ProjectContext { schemaVersion: number; architecture?: string; stack: string[]; importantModules: string[]; database?: string; integrations: string[]; conventions: string[]; commands: Record<string, string>; constraints: string[]; knownDecisions: string[]; agentsInstructions?: string; contextUpdatedAt: string; }
export interface ProjectThreads { schemaVersion: number; activeThreadId?: string; threads: Array<{ threadId: string; createdAt: string; lastUsedAt: string; label?: string; status: "active" | "inactive" }>; }
export interface ProjectPreferences { activeAgent?: string; profile?: string; model?: string; reasoning?: string; }
export interface ProjectState { schemaVersion: number; lastIndexedCommit?: string; lastWorkingTree?: string; preferences?: ProjectPreferences; }
export interface ProjectSnapshot { metadata: ProjectMetadata; context: ProjectContext; threads: ProjectThreads; state: ProjectState; reindexed: boolean; }
export interface ProjectContextEnvelope { projectId: string; projectName: string; stack: string[]; architectureSummary?: string; importantModules: string[]; databaseSummary?: string; integrations: string[]; conventions: string[]; commands: string[]; constraints: string[]; knownDecisions: string[]; agentsInstructionsAvailable: boolean; currentGitState: "clean" | "dirty" | "unknown"; }
